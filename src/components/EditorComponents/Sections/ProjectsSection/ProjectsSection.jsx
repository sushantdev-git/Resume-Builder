import Section from "../Section"

const ProjectsSection = ({sectionDetails}) => {
    return (
        <Section name="Projects" sectionDetails={sectionDetails}></Section>
    )
}

export default ProjectsSection;